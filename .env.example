# Server port
PORT=3000

# Paths to RSA key files (relative to project root)
# Run `node generate-keys.js` to generate these
PRIVATE_KEY_PATH=keys/private.pem
PUBLIC_KEY_PATH=keys/public.pem

# Actual secret data returned (as JSON string) when the correct public key is presented
SECRET_DATA={"api_key":"sk-prod-abc123xyz","db_password":"super$ecret!","admin_token":"eyJhbGciOiJSUzI1NiJ9.admin"}
